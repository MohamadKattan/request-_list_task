import 'package:flutter/material.dart';

class RestaurantTabels {
  RestaurantTabels(
      {required this.tabelNumber,
      required this.isOrdered,
      required this.tabelColor});
  int tabelNumber;
  bool isOrdered;
  Color tabelColor;
}
